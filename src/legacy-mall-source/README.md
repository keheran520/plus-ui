# legacy-mall-source

这里保存从 `ruoyi-shop` 复制过来的商城前端源码，作为迁移参考。

注意：

- 原始代码基于 Vue 2 + Element UI。
- 当前 `plus-ui-ts` 基于 Vue 3 + TypeScript。
- 因此前面这些文件目前不会直接挂载到现有路由中，只作为页面结构、字段、接口和交互逻辑的迁移基底。

后续迁移建议：

1. 先从商品、规格、订单、商户四块开始重写为 Vue 3 页面。
2. 再把它们改造成代练服务、套餐规格、代练订单、工作室管理。
